class CommonUtil:
    def give_me_a_Str(self) -> str:
        return """ returing a string """


print(
    f"will be runned once during import {__name__} == __main__ , if main it run as start up , else module name will printed")
